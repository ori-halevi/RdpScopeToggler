<UserControl x:Class="RdpScopeToggler.Views.TaskUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RdpScopeToggler.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Padding="5">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="החיבור פתוח!" FontSize="20" FlowDirection="RightToLeft" FontFamily="Bahnschrift" TextAlignment="Center" Foreground="Red" Margin="30" FontWeight="Bold" />
            <TextBlock Text="החיבור יסגר בעוד:" FontSize="20" FlowDirection="RightToLeft" FontFamily="Bahnschrift" TextAlignment="Center" />

            <local:AnyClockUserControl
                DataContext="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}"
                CountDownDay="{Binding CountDownDay}"
                CountDownHour="{Binding CountDownHour}"
                CountDownMinute="{Binding CountDownMinute}"
                CountDownSecond="{Binding CountDownSecond}"
                IsReadOnly="False" />

            <Button 
                Content="ניתוק מיידי"
                Padding="5"
                Margin="5 20"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                FontFamily="Bahnschrift"
                FontSize="20"
                Command="{Binding DisconnectCommand}"
            />

        </StackPanel>
    </Border>
</UserControl>
