<UserControl x:Class="RdpScopeToggler.Views.MainUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:materialDesignElevation="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
             
             xmlns:converter="clr-namespace:RdpScopeToggler.Converters"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <converter:EnumToBoolConverter x:Key="EnumToBoolConverter" />
    </UserControl.Resources>
    <Grid>
        <StackPanel>

            <!-- Tabs -->
            <materialDesign:ColorZone Mode="PrimaryDark">
                <Grid HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <RadioButton Content="ראשי"
                     Grid.Column="0"
                     HorizontalAlignment="Stretch"
                     HorizontalContentAlignment="Center"
                     GroupName="Tabs"
                     Style="{StaticResource MaterialDesignTabRadioButton}"
                     IsChecked="{Binding SelectedTab, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Home}" />

                    <RadioButton Content="רשימה לבנה"
                     Grid.Column="1"
                     HorizontalAlignment="Stretch"
                     HorizontalContentAlignment="Center"
                     GroupName="Tabs"
                     Style="{StaticResource MaterialDesignTabRadioButton}"
                     IsChecked="{Binding SelectedTab, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=WhiteList}" />

                    <RadioButton Content="פנימי"
                     Grid.Column="2"
                     HorizontalAlignment="Stretch"
                     HorizontalContentAlignment="Center"
                     GroupName="Tabs"
                     Style="{StaticResource MaterialDesignTabRadioButton}"
                     IsChecked="{Binding SelectedTab, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=LocalAddresses}" />
                </Grid>
            </materialDesign:ColorZone>



            <!-- Region -->
            <Border>
                <ContentControl prism:RegionManager.RegionName="CardsRegion" />
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
